<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Server Maintenance Process</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-900 text-yellow-300 p-8">
    <div class="max-w-4xl mx-auto">
        <h1 class="text-3xl font-bold mb-8">Server Maintenance Process</h1>
        
        <form id="maintenanceForm" class="space-y-8">
            <div id="step1" class="border border-yellow-300 p-4 rounded">
                <h2 class="text-xl font-semibold mb-4">[1] ServiceNow Maintenance Mode</h2>
                <div class="space-y-2">
                    <label class="block">
                        <input type="checkbox" name="openServiceNow" class="mr-2">
                        Open ServiceNow, All applications, Maintenance Mode - Add New
                    </label>
                    <label class="block">
                        <span class="block">Number:</span>
                        <input type="text" name="number" class="mt-1 block w-full bg-gray-800 text-yellow-300 border-yellow-300 rounded" placeholder="MMM*****">
                    </label>
                    <label class="block">
                        <span class="block">Maintenance Start:</span>
                        <input type="datetime-local" name="maintenanceStart" class="mt-1 block w-full bg-gray-800 text-yellow-300 border-yellow-300 rounded">
                    </label>
                    <label class="block">
                        <span class="block">Maintenance End:</span>
                        <input type="datetime-local" name="maintenanceEnd" class="mt-1 block w-full bg-gray-800 text-yellow-300 border-yellow-300 rounded">
                    </label>
                </div>
            </div>

            <div id="step2" class="border border-yellow-300 p-4 rounded hidden">
                <h2 class="text-xl font-semibold mb-4">[2] Create Snapshot {Azure}</h2>
                <!-- Add Azure snapshot form fields here -->
            </div>

            <div id="step3" class="border border-yellow-300 p-4 rounded hidden">
                <h2 class="text-xl font-semibold mb-4">[3-7] Server Updates and Validation</h2>
                <!-- Add server update and validation form fields here -->
            </div>

            <div class="flex justify-between">
                <button type="button" id="prevBtn" class="bg-yellow-300 text-gray-900 px-4 py-2 rounded hidden">Previous</button>
                <button type="button" id="nextBtn" class="bg-yellow-300 text-gray-900 px-4 py-2 rounded">Next</button>
                <button type="submit" id="submitBtn" class="bg-green-500 text-white px-4 py-2 rounded hidden">Submit</button>
            </div>
        </form>
    </div>

    <script src="/js/app.js"></script>
</body>
</html>
